load("@bazel_skylib//rules:build_test.bzl", "build_test")

build_test(
    name = "public_api",
    targets = [
        "//swiftformat:deps",
        "//swiftformat:load_package",
        "//swiftformat:swiftformat",
    ],
)
